name: Policy Testing
on: [pull_request, workflow_dispatch]
permissions: {contents: read}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pytest -q tests/test_fintech_policy.py tests/test_identity_policy.py tests/test_ai_policy.py
