name: Terraform Plan
on: [pull_request, workflow_dispatch]
permissions: {contents: read}
jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make tf-plan
      - uses: actions/upload-artifact@v4
        with: {name: tf-plan, path: terraform/environments}
